* Need to make the notepad save system adjust to directory name changes. 


* Need to make the text size be set to any number even if nothing is selected.


* Need to make the Qt text box expandable to the complete edge of the visual page.


* Need to refine MacOS's quicklook (need to be made much later)


* Need to optimize the startup time of the app so it opens as quick as possible.


* Need to add a settings button which gives option for dark visual page or change theme of the app.


* Add spelling checking feature and right click actions like look up meaning / pronunctation, could use system dictionary and spell checker. 


* Need to add a converter so a .note file could be easily converted to .md, .json, .txt, .yaml and etc formats, also add a button in "File" menubar for this. 


* Need to make the produced binary much smaller and increase startup time. Need to remove unnecessary Qt components compiled into the binary. 


* Need to reduce memory footprint when multiple and large docs are opened.


* Need to make tab bar scrollable when many tabs are opened. 


* When multiple opening docs, need to make the file picker allow opening of multiple docs simultaneously. 